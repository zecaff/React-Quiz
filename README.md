in this project the npm i json-server --save-dev dependency npm package was installed
to emulate an external API that returns the data in the data forlder

this server is executed by running the script npm run server
added in the package.json 